# SECURITY: Backend URL must use HTTPS in production
# CRITICAL: Add https:// protocol for security
VITE_BACKEND_URL=https://portbackend-sooty.vercel.app

# SECURITY: Owner UID from Firebase Console -> Authentication -> Users
# CRITICAL: Replace with your actual Firebase User UID
# This MUST match the UID in firestore.rules
VITE_OWNER_UID=KH3PGwdE7bcPH3mPbuJjX0bV4K13

# Viewer mode flag (UX only, not used for security)
VITE_VIEWER_IS_AUTHENTICATED=false
